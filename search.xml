<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Slurm集群上使用jupyter notebook的方法</title>
      <link href="/zh-CN/e966519e.html"/>
      <url>/zh-CN/e966519e.html</url>
      
        <content type="html"><![CDATA[<h2 id="一创建虚拟环境并安装jupyter-lab">一、创建虚拟环境并安装jupyterlab</h2><p>在集群上使用<code>conda creative -n &lt;your_envs&gt; python=3.10</code>来创建一个python虚拟环境并且指定python版本为3.10</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">conda create -n new_env_name python=3.10</span><br><span class="line">conda activate new_env_name</span><br><span class="line">pip install jupyterlab </span><br></pre></td></tr></table></figure><h2id="二编辑slurm脚本在计算节点上运行jupyter-服务">二、编辑slurm脚本，在计算节点上运行jupyter服务</h2><ol type="1"><li><strong>先在集群里面创建一个文件夹存放运行jupyterlab的slurm脚本。该脚本的位置即为jupyter lab的工作目录。</strong></li><li>创建一个slurm脚本运行<code>jupyterlab</code></li></ol><p>以下是一个slurm脚本运行jupyterlab 的例子:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"><span class="comment">#SBATCH --partition=gpu</span></span><br><span class="line"><span class="comment">#SBATCH --nodes=1 </span></span><br><span class="line"><span class="comment">##SBATCH --ntasks-per-node=1</span></span><br><span class="line"><span class="comment">#SBATCH --cpus-per-task=4   #对于r1,一共可以设80</span></span><br><span class="line"><span class="comment">#SBATCH --time=02:00:00</span></span><br><span class="line"><span class="comment">#SBATCH --job-name=DS       # 指定作业名称</span></span><br><span class="line"><span class="comment">#SBATCH --output=output_%j.out     # 输出日志文件</span></span><br><span class="line"><span class="comment">#SBATCH --error=error_%j.err       # 错误日志文件</span></span><br><span class="line"></span><br><span class="line">module load cuda/12.1.0</span><br><span class="line"></span><br><span class="line">conda info --envs</span><br><span class="line"><span class="built_in">source</span> activate deepseek</span><br><span class="line">conda info --envs</span><br><span class="line"></span><br><span class="line">jupyter lab --no-browser --ip=0.0.0.0 --port=8888 --NotebookApp.token=<span class="string">&#x27;&#x27;</span></span><br></pre></td></tr></table></figure><p>在<code>jupyter lab --no-browser --ip=0.0.0.0 --port=8888</code>中，</p><ul><li><code>--no-browser</code>表示计算节点运行jupyterlab后不会弹出浏览器。</li><li><code>--ip=0.0.0.0</code>表示所有的ip均可访问jupyter lab服务。</li><li><code>--port=8888</code>表示计算节点上的jupyterlab服务的端口是<code>8888</code>。</li><li><code>--NotebookApp.token=''</code>不配置token。删除这句表示添加随机token。</li></ul><ol start="3" type="1"><li>运行行该slurm脚本，在计算节点上运行jupyterlab服务。在<code>.err</code>文件里面记录了jupyterlab的服务信息，待会会用到。</li></ol><h2id="三创建本地映射本地运行jupyterlab">三、创建本地映射，本地运行jupyterlab</h2><ol type="1"><li>当运行完之后，使用<code>sequeue</code>查看一下jupyterlab在哪个计算节点。比如这里在<code>r1</code>节点。</li></ol><p><img src="../images/DH000002/1.png" /></p><p>然后需要知道<code>r1</code>节点的ip.直接在集群的终端中运行<code>cat /etc/hosts</code>查新<code>r1</code>节点的IP。</p><p>比如这里<code>r1</code>节点的ip是<code>211.66.36.30</code></p><p><img src="../images/DH000002/2.png" /></p><ol start="2" type="1"><li>现在需要记住该ip,并在本地windows的<code>Powershell</code>中粘贴运行,下面的<code>211.66.36.30</code>为计算节点<code>r1</code>的ip地址。</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh -L8888:211.66.36.30:8888 zfs_yang@211.661.36.241</span><br></pre></td></tr></table></figure><ul><li><code>-L</code>表示windows的本地。<code>-L8888:211.66.36.30:8888</code>表示windows本地的<code>8888</code>端口映射到<code>211.66.36.30</code>地址的<code>8888</code>端口。</li><li><code>zfs_y@211.661.36.241</code>即为集群的登录账号和ip。默认22端口，这里不做修改。</li></ul><p><strong>注意这里会要求输入集群的密码。建议将本地windows的公钥保存一份到集群的</strong><code>**~/.ssh**</code><strong>文件中，这样能够免输入密码，丝滑连接。具体方式这里不作说明。可以参考</strong><ahref="https://blog.csdn.net/savet/article/details/131683156"><strong>vscode通过ssh连接远程服务器+免密登录（图文）</strong></a></p><ol start="3" type="1"><li>然后，回到<code>.err</code>文件，找到类似于<code>[http://127.0.0.1:8888/lab?token=a7cd6deb3f6a3fc3754066a6e13ab00b55e20de87f17ba72](http://127.0.0.1:8888/lab?token=a7cd6deb3f6a3fc3754066a6e13ab00b55e20de87f17ba72)</code>(上面运行jupyterlabslurm脚本中配置了token才会有token)这个链接，注意token一定要复制，然后把这个链接粘贴到本地windows浏览器中打开即可，此时此刻即可在集群中你创建的虚拟环境中运行jupyterlab服务。没有token对应链接<code>[http://127.0.0.1:8888/lab](http://127.0.0.1:8888/lab)</code>.</li></ol><p><img src="../images/DH000002/3.png" /></p><p><strong>检查一下卡是否可以用：</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">check_available_gpus</span>():</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    检查并返回可用的NVIDIA GPU数量。</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="keyword">if</span> torch.cuda.is_available():</span><br><span class="line">        num_gpus = torch.cuda.device_count()</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;可用的NVIDIA GPU数量: <span class="subst">&#123;num_gpus&#125;</span>&quot;</span>)</span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(num_gpus):</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;GPU <span class="subst">&#123;i&#125;</span> 名称: <span class="subst">&#123;torch.cuda.get_device_name(i)&#125;</span>&quot;</span>)</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;未找到可用的NVIDIA GPU。&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 调用函数检查可用的GPU</span></span><br><span class="line">check_available_gpus()</span><br></pre></td></tr></table></figure><p><img src="../images/DH000002/4.png" /></p><p>注意事项：</p><ul><li><strong>目前jupyterlab只能配置使用1个节点，暂时没法同时使用两个节点。</strong>如果需要结束jupyterlab服务，只需要在slurm中使用<code>scancel</code>结束任务即可，不然不设置时间会一直占用集群资源。关闭本地的powershell窗口只会关闭本地的连接，重新开一个powershell窗口再运行上面的ssh即可重新恢复链接。此时原来的token链接还是可以用的。但是如果使用<code>scancel</code>结束jupyterlab服务，再次运行时候，token会发生变化，也就是链接会发生变化。但是token是可以配置固定的，这里不进行说明。</li></ul><h2 id="四jupyter-notebook更换内核">四、jupyter notebook更换内核</h2><p>理论上现在的jupyternotebook的内核是你在上面激活的虚拟环境。这时候可以参考：<ahref="https://juejin.cn/post/7321896126312300559">Python虚拟环境轻松配置：JupyterNotebook中的内核管理指南本文提供了一份详细指南，介绍了如何创建Py -掘金</a></p>]]></content>
      
      
      
        <tags>
            
            <tag> 教程 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Fourier级数与Fourier变换</title>
      <link href="/zh-CN/8ba9dd18.html"/>
      <url>/zh-CN/8ba9dd18.html</url>
      
        <content type="html"><![CDATA[<p>因为mathjax的CDN问题，数学公式可能显示有问题。可以尝试多刷新几遍网页。数学公式可以左右滑动~</p><h2 id="一函数的傅里叶级数展开">一、函数的傅里叶级数展开</h2><p>我们考虑一个以$2\pi$为周期的函数$ f(x) $,则有：$ f(x+2)=f(x)$.我们将其进行傅里叶级数展开有：</p><p><span class="math display">\[f(x)=c_0+a_1\cos x+a_2\cos 2x+...+b_1\sin x+b_2\sin2x+...= c_0+\sum_{n=1}^{\infty}a_n\cos nx+\sum_{n=1}^{\infty}b_n\sin nx\tag{1}\]</span></p><p>当我们用e指数展开时，根据欧拉公式有：</p><p><span class="math display">\[e^{i\phi}=\cos\phi+i\sin\phi \tag{2}\]</span></p><p>因此有： $$<span class="math display">\[\begin{equation}\left\{\begin{matrix}  \cos\phi = \frac{1}{2}(e^{i\phi}+e^{-i\phi})\\\\\sin\phi=\frac{1}{2}(e^{i\phi}-e^{-i\phi})\tag{3}\end{matrix}\right.\end{equation}\]</span> $$ 将式子(3)代入式子(1)有：</p><p><span class="math display">\[\begin{align}f&amp;=c_0+\sum_{n=1}^{\infty}\frac{1}{2}a_n(e^{inx}+e^{-inx})+\sum_{n=1}^{\infty}\frac{1}{2i}b_n(e^{inx}-e^{-inx})\\&amp;=c_0+\sum_{n=1}^{\infty}\frac{1}{2}(a_n-ib_n)+\sum_{n=1}^{\infty}\frac{1}{2}(a_n+ib_n)\end{align}\]</span></p><p>此时，引入标注 $ c_n = \sum_{n=1}^{\infty}\frac{1}{2}(a_n-ib_n) $ 和$c_n=\sum_{n=1}^{\infty}\frac{1}{2}(a_n+ib_n).$</p><p>傅里叶级数变为： <span class="math display">\[f=c_0+\sum_{n=1}^{\infty}c_ne^{inx}+\sum_{n=1}^{\infty}c_{-n}e^{-inx}\tag{4}\]</span></p><p>如果我们拓展n的范围即从$ n\rightarrow-n $则式（4）可以写成： <spanclass="math display">\[f(x)=\sum_{-\infty}^{\infty}c_ne^{inx}\tag{5}\]</span></p><h2 id="二fourier-级数的系数">二、Fourier 级数的系数</h2><p>从上面的步骤，我们已经获取了$ f(x) $复数形式的傅里叶级数。</p><p>即 $ f=c_0+c_1e^{ix}+c_2 e^{2ix} + …. + c_{-1} e^{-ix}+ c_{-2}e^{-2x}+.. + $</p><p>我们将根据以下步骤求解其系数$ c_0,c_1,….,c_n $</p><ul><li>利用公式$ \int_{0}^{2\pi} e^{inx}dx = 2\pi\delta_{n,0} $</li><li>方程两边同时乘以$ e^{-inx}\text{或} e^{inx} $,然后再进行积分.</li></ul><p>若求$ c_0 $： 两边直接从0到$2\pi$进行积分有:</p><p><span class="math display">\[\begin{align}\int_{0}^{2\pi}f(x)dx&amp;=c_0\int_{0}^{2\pi}dx+c_1\int_{0}^{2\pi}e^{inx}dx+c_2\int_{0}^{2\pi}e^{2inx}dx+..+...\\&amp;\Longrightarrow c_0=\frac{1}{2\pi}\int_{0}^{2\pi}f(x)dx \end{align}\]</span></p><p>若求$ c_n $则两边同乘以$ e^{-inx}$ 再对两边同时积分，即<spanclass="math display">\[c_n=\frac{1}{2\pi}\int_{0}^{2\pi}f(x)e^{-inx}dx\]</span></p><h2 id="三处理周期不为-2pi-的函数">三、处理周期不为$ 2\pi $的函数</h2><p>设$ f(x) $的周期为T，为了区分我们令$ X = T $，$ x= 2$.</p><p>则有：$ \frac{X}{x}=\frac{T}{2\pi}\Longrightarrow x= \frac{2}{T}X$</p><p>将此处的小x替换原来级数展开式(5)中的x,有：</p><p>$ f(x)= \sum_{-\infty}^ {\infty} c_n e^{\frac{inx}{T} x} $</p><p>为了方便的计算，我们让周期为某个数的二倍，即$ T=2l $</p><p>$ f=\sum_{\infty}^{\infty} c_n e^{\frac{in\pi}{l}x} $</p><p>$ c_n=\frac{1}{2l}\int_{-l}^{l}f(x) e^{\frac{-in\pi}{l}x}dx $</p><h2 id="四处理无周期结构的函数">四、处理无周期结构的函数</h2><p>处理无周期的函数的时候，我们假设其周期是无穷大，即$ l$.</p><p>我们设$ k_n=\frac{n\pi x}{l} $,在相邻的n中，$ \Delta n = 1 $,而$\Delta k = \frac{\pi}{l}\Delta n. $</p><p>我们可以将式子(5)$ f(x)=\sum_{-\infty}^{\infty}c_n e^{inx}$改写为：</p><p><span class="math display">\[\begin{align}f(x)&amp;=\sum_{-\infty}^{\infty}c_ne^{ik_nx}\cdot\Deltan\\&amp;=\frac{l}{\pi}\sum_{-\infty}^{\infty}c_ne^{ik_nx}\Delta k\end{align} \]</span></p><p>此时$ c_{k_n}=\frac{1}{2L}\int_{-l}^{l}f(x) e^{-ik_nx}dx $</p><p>显然，当$ l\rightarrow\infty $时，$ c_{k_n}\rightarrow 0 $,且$ f(x)$会发散（分子含有L）.</p><p>此时构造$ g_{k_n}=\frac{l\cdot c_n}{\pi} $,当$ l\rightarrow 0 $时，$g_{k_n} $才有机会收敛为某一个定值。</p><p>那么$ f(x)=\sum_{-\infty}^{\infty}g_{k_n} e^{ik_nx}\cdot\Delta k$</p><p>$ g_{k_n}=\frac{1}{2\pi}\int_{-l}^{l}f(x) e^{-ik_nx}dx $</p><p>将其写成连续的形式有：</p><p>$ f(x)=\int_{-\infty}^{\infty}g(k) e^{ikx}dk $</p><p>$ g(k)=\frac{1}{2\pi}\int_{-\infty}^{\infty}f(x) e^{-ikx}dx $.</p><p>此时，我们将函数$ g(k) $称为$ f(x) $的傅里叶变换.</p><p><ahref="https://www.physics.uoguelph.ca/chapter-9-fourier-transform">reference1</a></p>]]></content>
      
      
      <categories>
          
          <category> 磁学与数学基础 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> math </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>如何优雅地创建或管理Python虚拟环境</title>
      <link href="/zh-CN/a59a3594.html"/>
      <url>/zh-CN/a59a3594.html</url>
      
        <content type="html"><![CDATA[<h2id="为什么要创建虚拟环境不用虚拟环境行不行">为什么要创建虚拟环境？不用虚拟环境行不行？</h2><p>当然你可以不用虚拟环境。创建虚拟环境是为了方便管理我们本地的Python项目。比如我有两个项目A和B.A和B之间它们所需要的包依赖版本可能不一样，甚至python版本也不一样。如果两个项目混在一起运行，当项目A崩溃的时候可能会影响到项目B.此时虚拟环境就能很好地把它们隔离开，让两个项目的运行互不干扰。</p><h2 id="如何创建虚拟环境">如何创建虚拟环境？</h2><p>创建虚拟环境的工具有很多，这里主要介绍<strong>两种</strong>常见的创建方式。</p><h3id="python自带的方式创建pycharm中常见的方式">python自带的方式创建(pycharm中常见的方式)</h3><p>第一种是用python自带的工具创建虚拟环境。只要你下载的python版本大于等于3.3(目前最新的已经是3.13，隔了十来个版本)就可以直接用python自带的<code>venv</code>虚拟环境。这种方式是pycharm上创建python项目虚拟环境的默认方式。pycharm会在当前项目的文件夹中创建一个<code>venv</code>文件夹，这个文件夹就是该虚拟环境存放包的主要地方。你的python项目在哪个路径，虚拟环境就会在这个路径的<code>venv</code>目录下。pycharm创建一个项目的时候会默认创建这个文件夹，所以我们不用太担心。</p><p><img src="../images/DH00001/1.png" /></p><p>你要做的只需要去python官网下载一个python解释器，然后安装，配置好python环境。然后打开pycharm,在pycharm中新建一个项目，默认就会创建<code>venv</code>虚拟环境。如想删除虚拟环境，只需要把整个<code>venv</code>文件夹删除即可。</p><h3id="使用conda创建虚拟环境如果你不想用pycharm-推荐学习一下">使用conda创建虚拟环境(如果你不想用pycharm,推荐学习一下)</h3><p>不想使用pycharm,只想轻量地运行python项目。那么可以看一下~</p><p>conda是python的一个包管理器。可以用来给python项目安装依赖包以及创建管理虚拟环境。目前<code>Anconda</code>会自带<code>conda</code>,但<code>Anconda</code>是一个巨大的包，里面自带了很多python包，比如<code>numpy</code>等。比较臃肿。如果想使用<code>conda</code>不太建议用Anconda,推荐使用<code>Mini-anconda</code>,它是<code>Anconda</code>的精简版本，只包含了<code>caonda</code>管理器和一些必要的依赖。</p><p>如果想使用conda创建的虚拟环境，可以去Anconda官网或者一些镜像的网站下载安装<code>miniconda</code>。可以参考：<ahref="https://www.cnblogs.com/ajianbeyourself/p/17310681.html">miniconda的安装</a>。如果你装了Anconda也没关系的，<strong>下面的操作同样适用于Anconda。</strong></p><p>假如你是Windos用户，miniconda或者Anconda安装好了之后强烈推荐在<strong>开始</strong>菜单里面的<code>Anconda Prompt</code></p><p>或者<code>Anconda PowerShell Prompt</code>操作。</p><p><img src="../images/DH00001/2.png" /></p><p>点开你就会发现是一个黑乎乎的命令框(终端)。这就对了！有了这终端，你就可以和所有的conda环境进行友好地对话。</p><p><img src="../images/DH00001/3.png" /></p><p>括号里的<code>(base)</code>表明当前的python环境是<code>base</code>,括号里面显示的名字是什么，就对应哪个虚拟环境。比如现在显示base,就对应conda默认的环境base.此外你只需要掌握三个命令即可轻松对话conda以及操控它的虚拟环境。这三个命令下面将会一一介绍。</p><h4id="创建环境conda-create--n-你的虚拟环境名字"><strong>创建环境</strong>：<code>conda create -n &lt;你的虚拟环境名字&gt;</code></h4><p>把尖括号整体换掉你想要的名字即可。比如：<code>conda create -n myven</code>创建了一个名字是myven的虚拟环境。此时你也可以指定python版本，不指定则是默认conda自带的python版本（取决于你安装conda的版本）。指定很简单：<code>conda create -n myven python=3.10</code>(myven后面空格一下，输入python=3.10)指定了python3.10的版本。-n 表示name的缩写。</p><p><img src="../images/DH00001/4.png" /></p><p>这个过程会让你输入<code>y</code>回车确定，比如上图的圆圈2。</p><p>创建完之后，只需要按照提示激活环境即可。比如上图的圆圈3，提示你使用<code>conda activate myven</code>激活进入环境，用<code>conda deactivate</code>退出环境。你可以在任意环境中使用<code>conda activate myven</code>进入<code>myven</code>虚拟环境。激活其它环境只需要将<code>activate</code>后面的环境名字换掉即可。</p><p><img src="../images/DH00001/5.png" /></p><p>当我输入激活环境命令以后，旁边的<code>(base)</code>就换成了<code>(myven)</code>表示你已经成功进入这个虚拟环境。如果没出现则表示进入失败，一般不可能进入失败的，检查一下单词拼对了没有。在这个虚拟环境下（myven）,安装的包也会在这个环境中安装。哪个环境你就看括号里面的名称就行了，不复杂。</p><p>需要注意：<strong>当前激活的环境仅仅对当前这个命令窗口（终端）生效</strong>。你新开一个窗口或终端就会变成<code>base</code>环境(比如下图)。需要重新激活进入。除非你一直保留这个窗口。这个命令窗口(终端)在你不操作的时候比如我在当前窗口没有执行任何包的安装，且窗口没有程序命令输出，这个时候直接关闭命令窗口是不影响环境的，下次进来的时候重新激活一下环境即可。</p><p><img src="../images/DH00001/6.png" /></p><p>需要注意，<strong>一定要看清楚括号里面的是哪个环境</strong>，不然一不小心装进<code>base</code>环境。当然你也可以在cmd或者power shell中运行上述命令。但是power shell 或者cmd激活环境的时候<strong>很容易遇到初始化错误</strong>，因此最省事且最安全的方式还是直接在<code>Anconda Power Shell Prompt</code>或者<code>Anconda Prompt</code>中使用。同理，powershell或者cmd里面<strong>没有看到括号说明未激活进入虚拟环境</strong>，激活虚拟环境无论在哪个终端，你都会看到一个括号，包括前面提到的<code>venv</code>。没看到括号就说明当前的命令窗口(终端)没有激活环境。此时不要轻易执行python包的安装命令。</p><h4id="查看虚拟环境有哪些conda-env-list"><strong>查看虚拟环境有哪些</strong>:<code>conda env list</code></h4><p>这是你需要掌握的第二个命令，查看一下有哪些虚拟环境，比如时间久了你忘记了有哪些虚拟环境。或者虚拟环境名字一下子记不住，但想激活，即可使用这个命令查看。</p><p><img src="../images/DH00001/7.png" /></p><p>此时左边的表示虚拟环境的名称，右边对应虚拟环境保存的路径。星号<code>*</code>表示当前所在的虚拟环境。如果你想更改虚拟环境的目录，请看这里：<ahref="https://zhuanlan.zhihu.com/p/649072923">【知乎】anaconda的envs路径跑到c盘了，修改为D盘</a>。一般我觉得没必要改，除非你内存不够。</p><p>如果你想删除这个虚拟环境，直接跑到目录下删掉即可，然后<code>conda env list</code>就不会显示。还有没有更优雅的删除方式？当然有！</p><h4id="删除虚拟环境conda-remove--n-xxxxx名字---all"><strong>删除虚拟环境</strong>：<code>conda remove -n xxxxx(名字) --all</code></h4><p>比如<code>conda remove -n myven --all</code>。即可优雅的删除虚拟环境<code>myven</code>。这是第三个命令。</p><p>前提是你要退出这个虚拟环境<code>conda deactivate</code>一下再执行。不然删除失败。这个过程同样需要你输入<code>y</code>回车确认。</p><p><img src="../images/DH00001/8.png" /></p><p>此时你再使用<code>conda env list</code>就会发现已经没有<code>env</code>环境了。</p><p><img src="../images/DH00001/9.png" /></p><p>因此你只需掌握上面三条命令以及延伸生出来额外两条激活与退出命令即可优雅轻松的使用你的虚拟环境。</p><p>快速总结以及复习一下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">conda create -n myven <span class="comment">#创建一个名字是myven的虚拟环境,-n是name名字的缩写。</span></span><br><span class="line">conda activate myven  <span class="comment">#激活myven虚拟环境，可以在任意环境下使用。</span></span><br><span class="line">conda deactivate <span class="comment">#退出环境，在窗口没有工作时，可以直接关闭窗口退出。</span></span><br><span class="line">conda <span class="built_in">env</span> list   <span class="comment">#查看有哪些虚拟环境以及它们所在的目录。</span></span><br><span class="line">conda remove n myven --all <span class="comment">#删除myven虚拟环境</span></span><br></pre></td></tr></table></figure><h4 id="minniconda-换源">minniconda 换源</h4><p>为什么要换源？因为默认官方的源下载依赖包的时候特别慢，有可能需要fanqiang.</p><p>此时直接访问清华源：<ahref="https://mirrors.tuna.tsinghua.edu.cn/help/anaconda/">https://mirrors.tuna.tsinghua.edu.cn/help/anaconda/</a></p><p>根据提示把下图圈1的代码粘贴到圈2的文件中去，然后回到conda命令行刷新一下即可。</p><p><imgsrc="../images/DH00001/1.png" />即：先找到<code>.condarc</code>文件，</p><p><imgsrc="../images/DH00001/11.png" />若是找不到这个<code>.condarc</code>文件(一般是能找到的)，可以在<code>Anconda Power Shell Prompt</code>终端中执行：</p><p><code>conda config --set show_channel_urls yes</code>即可像这样：</p><p><img src="../images/DH00001/12.png" /></p><p>打开<code>.condarc</code>后粘贴从刚才网站复制的内容然后按<code>crtl</code>+<code>S</code>保存:</p><p><img src="../images/DH00001/13.png" /></p><p><code>.condarc</code>会有原来的源链接内容，可以全部删除。不用纠结删除会怎么样。就一个源而已。</p><p>回到<code>Anconda Power Shell Prompt</code>终端，关闭了就重开一个，没什么大不了的。刷新源一下：输入</p><p><code>conda clean -i</code>回车后提示输入y然后回车继续，即可使用清华源。其它源也一样。建议：<strong>换源的时候一定要去官方的源网站去换，比如刚才的清华源链接。不要求其他论坛比如CSDN上去换，那边的源有可能链接是旧的。</strong></p><p><img src="../images/DH00001/14.png" /></p><h4id="在pycharm中使用conda虚拟环境有bug但不影响整体使用">在pycharm中使用conda虚拟环境（有bug,但不影响整体使用）</h4><p>没错，你没看错，pycharm同样支持使用conda的虚拟环境。非常简单！</p><p>现在我举个例子：我先创建一个<code>myven2</code>的虚拟环境：开始菜单里面打开<code>Anconda PowerShell prompt</code>输入<code>conda create -n myven2</code>创建好了就行，先不用激活。</p><p>现在还记得上面找虚拟环境的路径的命令吗？对就是<code>conda env list</code>待会要用到</p><p><img src="../images/DH00001/15.png" /></p><p>现在打开pycharm.你可以新建一个项目，或者用原来的项目，这个都没事。点击右下角的虚拟环境，然后选择<code>Add New Interpreter</code>添加新的解释器。原因是一个虚拟环境对应一个python解释器。</p><p><img src="../images/DH00001/16.png" /></p><p>之后选择侧边选择<code>Conda Environment</code>然后选择现有的环境，最后下拉找到刚才创建的环境<code>myven2</code>即可。最后点击<code>ok</code>.</p><p><imgsrc="../images/DH00001/17.png" />现在右下角就会显示虚拟环境为<code>myven2</code>.如果列表没显示，就需要手动输入conda的安装路径查找。</p><p><img src="../images/DH00001/18.png" /><strong>bug:千万不要使用Pycharm自带的终端启动或安装任何东西！(仅仅针对conda创建的虚拟环境下)</strong>。因为终端里面的PS(下图第一条红线PS)左边不显示<strong>不显示括号+虚拟环境</strong>的名字。所以这个终端肯定是没有激活虚拟环境的。但是不影响在pycharm中安装依赖包。pycharm中安装其它包是会安装到当前虚拟环境<code>myven2</code>下的，但是终端并不支持，可能是bug。<strong><font style="color:#DF2A3F;">但没关系，我们可以使用刚才的</font></strong><code>**&lt;font style="color:#DF2A3F;"&gt;Anconda Powershell Prompt&lt;/font&gt;**</code><strong><font style="color:#DF2A3F;">中激活</font></strong><code>**&lt;font style="color:#DF2A3F;"&gt;myven2&lt;/font&gt;**</code><strong><font style="color:#DF2A3F;">环境来代替这个终端。</font></strong>比如说安装或者启动jupyterlab，我们可以在<code>Anconda Powershell Prompt</code>中先激活对应的环境，然后再执行相关的安装/启动命令。</p><p><img src="../images/DH00001/19.png" /></p><p>如果是使用python自带的虚拟环境<code>.ven</code> PS左边会有括号+虚拟环境名字venv这才是这个终端激活了虚拟环境。没有的一律没激活虚拟环境。</p><p><img src="../images/DH00001/20.png" /></p><p>尽管conda 在pycharm中存在这个bug, 但这无妨，我们还是可以了解一下conda的虚拟环境是如何使用的。</p><h2 id="建议">建议：</h2><p>装了pycharm没必要再装miniconda。除非你电脑内存不足。其实pycharm更加智能一点，但也会有bug，比如图片弹不出来，但可以设置保存图片。如果你想体验Vscode或者不想用pycahrm,只想轻量运行python项目那就建议装conda.其实运行jupyternotebook也是不需要安装conda,pycharm和浏览器中也可以运行notebook,但是pycharm需要专业版。浏览器上没有代码补齐的提示。</p><p>但是在服务器里面是用不了pycharm的，操作也是类似的，服务器上使用conda更加方便，只需要安装miniconda的<code>.sh</code>文件（不需要root）,然后配置一下conda环境,把conda加入进系统环境。再结合上面的三条命令基本没问题。可以参考：<ahref="https://www.cnblogs.com/netWild/p/18289735">https://www.cnblogs.com/netWild/p/18289735</a></p><p>如果无法无法运行.sh文件，则赋予其执行权限<code>chmod +x xxx.sh</code>然后再运行<code>./xxx.sh</code></p>]]></content>
      
      
      <categories>
          
          <category> Python </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 教程 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
